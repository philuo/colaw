// Electrobun release pin: beta track — the desktop shell uses beta-only
// window/webview APIs. Exact versions only; npm tags are not selectors.
const config = {
  electrobun: {
    version: "2.0.2-beta.19",
  },
} as const;

export default config;
