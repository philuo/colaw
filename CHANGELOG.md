# 更新日志（Changelog）

本项目的所有重要变更均记录于此，最新在前。
每个条目随 `vX.Y.Z` 标签发布；发布流水线会创建对应的 GitHub Release，
附带 DMG、应用 zip 与自动更新 feed。

## [1.0.8] — 2026-09-13

### 新增

- **内置 MCP 工具集（法律与企业信息检索）**：产品内置 18 个远程 MCP 服务，
  开箱即用，无需用户配置——北大法宝 8 个（法规检索/法规关键词/案例语义检索/
  案例关键词/法条检索/法律识别/案号识别/引注校验）、企查查 9 个（工商信息/
  经营风险/知识产权/企业经营/历史信息/股东高管/司法法规/司法案件/招投标）、
  Firecrawl 1 个（网页抓取与结构化提取）。全部经 Streamable HTTP 接入，工具以 `mcp__<server>__<tool>` 形式对模型可见。
  服务密钥**不随产品分发**：在「设置 → 插件 → 内置插件」中逐项粘贴（经受管凭证
  存储保存于 ~/.colaw，永不写入配置文件或随应用分发），北大法宝/企查查/Firecrawl
  保存后重启应用生效；未配置密钥时服务照常挂载，调用时返回提供方的 401。
- **MinerU 文档解析工具（`mineru_parse_document`）**：把工作区里的 PDF（含扫描件）、
  图片或 Office 文档经 MinerU 云服务解析为 Markdown——上传（预签名 URL）→ 轮询 →
  结果包内提取 `full.md`，写回源文件旁 `<name>.mineru.md`，模型即可用文件工具读取；
  支持 OCR 开关与语言提示。密钥经「设置 → 插件 → 内置插件 → MinerU」配置（MINERU_API_KEY），**每次
  调用实时解析，填入即生效**。
- **百度网盘技能（baidu-drive）**：内置百度网盘 Agent Skill（上游
  baidu-netdisk/bdpan-storage，Apache-2.0，含参考文档与 bdpan CLI 安装/登录
  脚本），支持上传、下载、转存、分享、搜索、整理网盘文件，首次使用自动引导
  安装与登录授权。
- **预览五项交互修复（PDF / Office）**：① **文字可划选**——PDF 每页叠
  加 pdfjs 文字层（与画布像素对齐、随缩放同步），docx/pptx 修复应用级
  禁选规则压住库文字层的问题（可选区域标记），四类文件均可高亮选中、
  复制。② **PDF 支持 ⌘/Ctrl+滚轮缩放**——与图片预览同款连续指数手感
  与光标锚点，手势期间经 CSS 变量瞬时缩放，停手后按缩放倍率重绘位图
  保证清晰；缩放状态随 tab 记忆。③ **docx 手势缩放卡顿修复**——手势
  期间改为 GPU transform 预览（零布局开销），停手 160ms 后一次提交真
  实缩放并恢复光标锚点，不再逐事件触发库的全量重排。④ **xlsx 合并
  单元格整格选中**——点击合并区域任一位置，选区自动扩展为整个合并
  格（拖拽多选不受影响）。⑤ **内存释放策略**——关闭 tab 立即释放
  （随记录回收），切换 tab / 隐藏右侧栏 30 秒后释放内容（视图状态保
  留），切回时自动重读并回到上次阅读位置；Office 滚动视图接管面板
  滚动口，位置恢复与文本预览同源。
- **预览选区圈定在当前 tab**：在图片（OCR 文字层）、Office、PDF 预览中拖选
  或 ⌘A 全选时，选区只覆盖**当前使用的 tab** 内的预览内容——不会带出应用
  其他界面（对话、侧栏）的文本；双栏（拆分）模式下只选中鼠标所在/焦点
  所在那一栏的 tab，另一栏不受影响。实现为每个预览 tab 挂载选区作用域：
  ⌘A 按焦点或指针位置解析到当前作用域后圈定全选（输入框等可编辑元素的
  全选不受影响）；拖选越界时把 Selection 实时裁剪回该 tab 容器与选区的
  交集。
- **修复 stable 应用白屏打不开（严重）**：07:57 起的 stable 包启动即白屏。
  根因链：内置 MCP 集成把 `@modelcontextprotocol/sdk` 的依赖
  `zod-to-json-schema` 拉入运行时，而该包使用 npm 老式「裸条件 exports」
  （`{"import":…,"require":…}` 整体即 `.` 入口的简写）；打包闭包的
  `entryOfManifest` 只认 `./` 路径键的 exports，解析失败后按"可选依赖
  不发货"处理，产物缺包 → mcp-keyring 导入失败 → cordis 组合树挂载
  失败 → 窗口停在 splash。该缺口自上游第一版即存在（更早版本的打包
  日志就印有 `unshipped optional imports: … zod-to-json-schema`，与
  sharp 平台件混在一列从未被注意），此前无运行时引用故无害。修复
  （additive 12 行）：`entryOfManifest` 识别裸条件简写；同源修复
  `declaredExportSpecs` 把条件键当子路径拼出幽灵包名（`execaypes`）的
  问题。**验收方式升级**：打包后必做 /tmp 副本 + 隔离 HOME 启动冒烟
  （本轮全程执行：修复前 2 项加载失败，修复后 0 项，窗口正常载入）；
  另查实「从仓库 build 目录直接启动」为不可信环境（同一 app 在仓库
  内启动复现 mcp-keyring 解析失败、拷出后 0 失败，与 ~/.colaw 状态无
  关）。顺带补回泄漏清理时误删的百度网盘技能组合行。
- **预览测试链路修复 + 资源审计收口**：`pdf-license-bundle` 既有失败根因
  查明——spec 以 `npm_execpath` 推断包管理器，bun 运行时落到 `bun pack`
  （被当作脚本名）报 `Script not found "pack"`；改为 bun 原生
  `bun pm pack` + tarball 目录发现（bun 1.4.2 的 `--pack-destination`
  不生效、stdout 行序随 TTY 变化，故以目录为准），断言改走 tar 列表。
  预览包 **300/300 全量通过**，"已知环境失败"清单移除该项。资源释放
  补齐：PDF 缩放落定定时器随组件卸载清理；选区作用域补 pointercancel
  结束拖选（避免手势中断后拖选状态滞留）；文档监听随最后一个作用域
  注销整体拆除。
- **通用设置新增「身份预设」**：两个输入项——AI 人设（默认"严谨、细心、
  程序员冲哥"）与用户人设（默认"山东济南律师（琪琪）"）。写入 `ui-identity`
  设置命名空间并实时发布为系统提示词 `identity:preset` 段（紧随部署 persona
  之后），下一次请求即生效；两栏清空即关闭该段。**持久性**：系统提示词每一步
  请求都重新组装并作为会话首条 system 消息发送，而会话压缩的范围选择永不改写
  首条 system 消息、压缩后还会用最新组装结果重写该节点——身份预设因此真实固定
  在上下文中，不受压缩与长对话影响（含宿主侧组装断言测试）。设置行样式修正
  （标签不折行，与周围风格一致）。
- **Office 文件直读预览（Word / PowerPoint / Excel）**：右侧栏文件预览
  新增 `.docx`、`.pptx`、`.xlsx`/`.xlsm` 渲染器，采用纯前端方案
  `@silurus/ooxml@0.87.0`（锁定精确版本；Rust/WASM 解析 + Canvas 渲染），
  **无需 LibreOffice 转换、零服务端依赖**。xlsx 支持多 sheet 标签、冻结
  窗格、合并单元格、条件格式与图表；解析器 WASM 随包 base64 内联、worker
  走库内置的自包含回退，全程离线可用。已知边界（来自官方能力表）：
  SmartArt/3D 效果、数据透视表不支持；docx 批注仅解析不内联渲染；旧二进制
  格式（.doc/.ppt/.xls）不支持。预览插件载荷因此增大约 10MB（本地加载）。
  **阅读体验对齐 PDF 预览**：docx/pptx 采用库的连续滚动视图（多页连续滚
  动浏览，不再是单页翻页）；默认宽度占满面板且随面板宽度自动重适配，比例
  恒不拉伸；**文本可直接划选复制**（文字层随页面渲染）；**超链接可点击**，
  外部链接经桌面桥接用默认浏览器打开，文档内跳转由视图内部处理；**⌘/Ctrl+
  滚轮缩放**复刻图片预览的连续指数手感（0.007 灵敏度、单事件 0.5–2 钳制）
  并以光标为缩放锚点，范围 0.25×–8×；xlsx 网格同样支持捏合缩放。
- **模型输入类型：声明、配置与「视觉」徽标**：模型能力从
  `text | image` 扩展为 `text | image | video | file`，对齐
  GLM-5.3-Flash 等多模态模型的官方输入面（视频 `video_url`、文件
  `file`，均经官方 Chat Completions 文档核对）。设置 → 模型的每个
  模型行新增「输入类型」勾选组（文本恒选；图片/视频/文件可勾），
  未声明的行显示宿主解析出的生效状态并注明"沿用提供方自身声明"，
  勾选即把完整数组显式写入该行（deepseek 路由 `inputModalities`、
  pi-ai 路由 `input`）；「添加模型」新行默认声明 `text + image`，
  不再把新模型当纯文本对待。pi-ai 路由级默认输入同步改为
  `[text, image]`；pi-ai 的 Model 仍只携带其序列化器能发出的
  wire 模态，`video`/`file` 经旁路映射直达模型信息，为下一步
  原生文件/视频请求做准备。模型目录透传 `inputModalities`，
  聊天输入区的模型按钮在当前模型支持图片时显示「视觉」徽标
  （`视觉`/`Vision` 双语）。`llm.listModels` 升级为 Remote，
  供设置页读取每个模型的生效能力；`llm-replay` 配置校验放宽到
  新词汇。**注意：文件与视频仍走"句柄文本"路径（模型用文件工具
  自行读取）——请求体原生携带 GLM `file_data`/`video_url` 是
  下一阶段**。

### 性能

- **拖动右侧栏不再整面板重渲染**：此前拖动分隔条的每一帧都会经由
  store 状态重渲染整个右侧栏 React 子树（Dock、标签页、预览内容全部
  跟着重渲），这是拖动抖动的主因。现在面板宽度经帧级 CSS 变量
  `--dsh-rightbar-width` 直接下发，右侧栏子树在拖动期间被冻结在
  稳定键上——每帧只有网格轨道与变量更新，纯布局工作，面板内容零
  React 开销。
- **图片平移/捏合手势走 GPU 直通道**：pointermove 与滚轮事件不再经
  React 状态（此前每事件一次组件重渲染）。手势期间变换矩阵直写
  DOM，并以 `will-change: transform` 把图片提升为独立合成层——平移
  与捏合变为纯 GPU 纹理移动；静止时移除合成层，按布局尺寸原生栅格
  化保持锐利。手势停止 160ms 后状态才落回 React（徽标等派生 UI 同
  步）。普通滚轮平移同享此通道。
- **长会话滚动跳过屏外布局与绘制**：消息行启用
  `content-visibility: auto`（以最后一次渲染尺寸参与滚动条计算），
  屏外消息不再参与布局与绘制，长对话滚动交给合成器。

### 修复

- **长图/大图显示模糊**：根因是 `object-fit: contain` 的绘制路径——
  WebKit 在 Retina 下对大位图按降质分辨率栅格化。现在元素盒直接取
  contain 数学算出的精确尺寸（布局尺寸化，走浏览器原生图片渲染路径），
  比例、完整性、量化拖动等保证全部保留；同时手势期间为流畅而提升的
  合成层会在手势提交后立即移除，避免把图层钉死在缩放前的栅格分辨率。
- **图片文字可直接选中复制（微信式，无感识别）**：打开图片即在后台静默
  调用 Apple Vision 识别文字（经系统 `osascript` + AppleScriptObjC 桥驱动
  `VNRecognizeTextRequest`，中文准确率高，**零新增二进制、零打包脚本改动**；
  JXA 桥在 macOS 15 已损坏，故用 ASObjC）。识别结果铺成与图片像素对齐的
  **透明可选文字层**：不显示任何按钮、框线或弹窗，鼠标可直接划选、⌘C 复制，
  文字层随缩放/平移（含深度缩放路径）同步变换；重复打开同一文件走缓存不再
  识别；SVG 与极小图跳过；识别失败静默无打扰。文字同时进入辅助功能树。
- **高倍放大永远锐利（深度缩放）**：放大超过层预算（显示侧最长轴
  4096 设备像素）后，预览自动切换为视口大小的 canvas 深度缩放路径——
  GPU 只把可见区域按显示分辨率采样进一块与面板等大的背板（Preview.app
  同款思路），任何倍数下都清晰、内存有界；缩回预算内则恢复 transform
  路径。此前超过纹理预算的放大会被 WebKit 钳制栅格化，停手也回不来。
- **布局反馈环护栏**：帧宽度测量以窗口内宽为上限。此前内容瞬时溢出
  会把被撑大的帧宽回灌为"视口"，让步逻辑随之追逐增长，且被污染的
  宽度随布局持久化，重启后右侧栏渲染到窗口之外。
- **xlsx 合并单元格交互修正**（按真机反馈）：移除上一版自绘的合并区
  悬浮描边框——它在网格平移/缩放后会与单元格边界错位，且并非产品应
  有的视觉语言；悬浮恢复为与普通单元格一致的默认表现。点击（或键盘
  导航）进入合并区的**整格选中**保留，由库自身的选区渲染绘制，与网格
  严格对齐。
- **预览缩放后选区被清空**（PDF 最难用的一点）：根因是缩放停手后的
  清晰重绘会整页重建文本层，刚选好的文字随即消失。现在文本层与画布
  位图解耦——每页只构建一次、随页面盒缩放，重绘不再触碰它，缩放前后
  选区完整保留且随文字同步缩放。
- **拖选与浏览器引擎打架**：拖选越出预览栏边界时，此前在拖拽过程中
  就实时改写选区（重设 Selection 会摧毁引擎的拖拽锚点，表现为选区
  跳动、回缩、无法平滑扩展）。改为让原生拖拽自然完成，**指针抬起时**
  一次性把选区收敛到当前 tab 容器内；选区作用域的越界保证不变。
- **stable 应用启动后白屏直到首次点击**（严重）：窗口先以空白占位
  页 orderFront，WKWebView 首个内容帧提交后窗口服务端未收到显示通道，
  应用其实已完整渲染（可直接交互）却白屏——用户感知为"启动极慢/打不开"。
  修复：主 webview `dom-ready` 时补一次 `show()`（幂等，每次导航一次），
  冷启动零交互即出内容（三轮实测）。
- **内置 MCP 移除企查查「历史信息」**（`qcc-history`）：该服务需企查查
  账号单独授权（真机返回 `300012 当前用户未授权使用历史信息服务`），
  产品不再内置，避免给用户一个必然失败的工具；其余 17 个服务不变。
- **剪贴板写入在 WKWebView 被拒时的兜底**：xlsx 复制与部分复制按钮
  依赖异步 Clipboard API，宿主（WKWebView）可能以权限/用户激活原因
  拒绝，且拒绝时复制静默失败。新增桌面桥接写入——页面经 preload 桥把
  文本交给宿主，宿主用原生剪贴板写入并回执；异步 API 成功时不影响
  原路径。
- **DMG 拖拽安装窗口样式恢复**：DMG 从 v1.0.1 起丢失了安装界面样式
  （背景水印、图标定位的拖拽引导窗口）。恢复 `scripts/dmg-background.swift`
  背景生成器，并重写 `scripts/build-dmg.ts`：以 Hutch 官方 DMG 为唯一
  内容源（应用载荷与更新链保持一致），在其上重新排布 Finder 窗口
  （460×345、COLAW 水印背景、Colaw.app → Applications 图标位与箭头），
  再压缩签名；官方更新链（patches / update.json）不受影响。

## [1.0.7] — 2026-09-12

### 重构

- **图片默认完整显示改为 CSS 结构性保证**：不再由 JS 测量面板计算"适配
  比例"（该路径在标签页恢复等布局竞态下可能测错，导致默认被裁或缩不小
  回完整视图）。默认姿态由 `object-fit: contain` 全权负责——整图居中完整
  可见、宽高比由渲染引擎结构性保证，右侧栏宽度变化只做等比重适配，绝
  不拉伸；缩放只是在 contain 之上叠加 transform，下限 k=1 恰好就是完整
  视图，捏合缩小永远能回到整图。
- **静止画面保持锐利**：静止时不附加任何 transform（也不再常驻
  will-change 合成层），浏览器按布局尺寸原生栅格化；仅在缩放/平移发生
  时才叠加 transform。
- **缩放灵敏度三连调**：0.002 → 0.004 → 0.007（触控板捏合约快 1.75 倍，滚轮一档触达单事件 ×2 上限），
  捏合跟手；普通滚轮仅在放大后图片超出面板时平移。
- 徽标百分比与平移钳制全部来自 contain 纯数学（面板尺寸 + 原始宽高
  比），与渲染结果不可能不一致。

### 修复

- **拖动右侧栏分隔条时卡顿**（长图/大图/PDF 打开时）：此前图片与 PDF
  页面的显示宽度逐帧跟随面板，拖动期间每帧都对数百万像素内容重新采
  样/重合成。现在内容显示尺寸经过统一的"重采样节律器"量化更新——拖
  动期间以约 120ms 一档步进，停顿后 150ms 内落到精确终值；object-fit
  的比例保证与缩放行为不受影响。

## [1.0.6] — 2026-09-12

### 修复

- **图片默认仍可能显示不全（会话恢复场景）**：恢复的标签页在容器布局
  未稳定时挂载，组件初次测量拿到被撑高的面板后，fit 下限被永久抬高，
  之后无论怎么捏合都缩不回完整视图（实测 720×1584 的图片卡在 77%，
  下半截出窗）。现在适配比例的除数以窗口可视区为最终真相——面板测量
  钳制在"窗口底边减框架顶边"之内，无论祖先布局如何劣化，默认姿态都
  保证整图可见；同时在挂载、动画帧、延时与窗口缩放时多轮重测，且只要
  操作者未手动缩放，面板修正即自动重新适配（手动缩放后则保持用户状态
  不被覆盖）。
- **缩放阻尼感偏强**：灵敏度系数由 0.002 调至 0.0025，滚轮一档约
  1.28 倍（原 1.22），捏合连续缩放更跟手。

## [1.0.5] — 2026-09-12

### 新增

- **图片预览聚焦缩放**：右侧栏图片预览不再只是"平铺看个轮廓"。默认姿态
  改为完整居中显示（等比缩放至面板内、且不超过原始像素，避免小图模糊放大）；
  ⌘/Ctrl + 滚轮（触控板双指捏合的等效手势）以指针为锚点缩放，放大后可
  按住拖拽平移（自动钳制在图片边缘内，不会拖飞），双击在"适配"与"2 倍"
  之间切换，右下角新增百分比徽标，点击即可一键复位（处于适配比例时徽标
  置灰，表示当前无可复位）。缩放/平移仅为视图状态，不改动文件本体。
  新增 18 个交互用例覆盖：居中适配、禁止超额放大、滚轮缩放锚点与幅度
  比例、平移边缘钳制、双击切换与徽标复位。

### 修复

- **长图默认显示不完整**：图片按原始像素参与布局，会把预览视口撑高，
  "适配比例"随之按被撑大的错误面板计算（实测 3174×8130 的长图按 50%
  显示、下半截被裁）。现在视口以绝对定位铺满定界后的面板（对齐代码
  预览的既有方案），任何长图都按真实面板测量，默认完整可见。
- **缩放过灵敏、不跟随指针**：原先每个滚轮事件固定 ×1.25，触控板捏合
  以高频小事件涌入，0.5 秒即可放大数百倍；且锚点计算使用了被撑大的
  面板中心，缩放会"飞离"指针。现在缩放系数与手势像素幅度成正比
  （滚轮一档约 1.27 倍，捏合平滑连续，单次事件钳制在 ×0.5–×2），锚点
  始终取实时面板下的指针位置。
- 测试设施：PDF 兼容性回归的 worker 载体在 Node 22 进程池下补齐
  `Uint8Array.prototype.toHex` 平价垫片（应用目标 WKWebView 均原生具备，
  生产路径不受影响），本地与 CI 进程池均可复现真实故障形态。

## [1.0.4] — 2026-09-12

### 修复

- **桌面端 PDF 预览打开即失败**（`this.#g.getOrInsertComputed is not a
  function`）。PDF.js 6 依赖 Map upsert 提案
  （`getOrInsert`/`getOrInsertComputed`），而应用所在的 WKWebView 并不
  支持（旧版 macOS 上还缺 `Promise.try` 与 Set 方法），导致每次打开 PDF
  都在解析阶段报错。现在预览在 worker 边界两侧同时安装幂等垫片：页面
  侧在加载 PDF.js 之前引入 `pdf/compat.ts`，同一份垫片源码也会拼接在
  worker 的 Blob 模块之前。新增回归用例在"两个 realm 都删掉这些 API"
  的前提下解析真实 PDF，完整复现桌面端故障形态；25 个 PDF 用例全部
  通过。并已在桌面应用中端到端实测：右侧栏成功渲染示例 PDF 页面。

## [1.0.3] — 2026-09-12

### 修复

- **回收站"清空全部"对本次运行中打开过的会话一条也删不掉。**
  `deleteArchivedSession` 之前会拒绝宿主会话仓库仍持有的会话，但归档
  并不会把它从仓库中逐出，于是这个存活探测恰好挡住了回收站本来就该
  删除的会话——清空操作在第一条就带着 `workspace/trash-conflict` 中止。
  现已移除这条无法成立的探测；`clearTrash` 逐条独立清理（单条失败不再
  连累其余条目），并将幸存条目汇总为一个冲突错误上报。宿主侧新增四个
  回归用例。

## [1.0.2] — 2026-09-12

### 新增

- 新建会话页的 Codex 风格工作区胶囊：绑定态展示文件夹图标与标题，悬停
  显现实心圆叉移除入口；未绑定态保留已有工作区选择，但不再提供"不绑定
  工作区"独立行，也没有添加目录（Finder）入口。新建会话默认不选工作
  区——只有侧栏工作区分组操作、或当前会话所在工作区的新建动作才会预选。
  移除或切换绑定工作区时，未提交的草稿内容随行保留。启动时复用或新建
  一个不绑定工作区的空白会话。
- `scripts/pack-stable-release.ts`：本地一键稳定版打包链，对齐 CI
  （打包 → 官方 `--env=stable` 身份 → DMG），产物 `build/stable-macos-arm64/`
  中留下可直接运行的 `Colaw.app`（每次重打包不再弹自动解压弹窗）与官方
  `Colaw.dmg` 并列，且本地不写入更新 feed。
